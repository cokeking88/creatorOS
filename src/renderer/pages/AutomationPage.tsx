import React,{useState} from 'react'; import type { AppState } from '../../shared/types';
export function AutomationPage({state,refresh}:{state:AppState|null;refresh:()=>void}){const[name,setName]=useState('Open dashboard');const[cron,setCron]=useState('0 9 * * *');const[url,setUrl]=useState('https://www.google.com');return <div className="page"><h1>Automation</h1><div className="content-grid"><section className="panel"><h2>Create cron job</h2><input className="field" value={name} onChange={e=>setName(e.target.value)}/><input className="field" value={cron} onChange={e=>setCron(e.target.value)}/><input className="field" value={url} onChange={e=>setUrl(e.target.value)}/><button className="primary" onClick={()=>window.creatorOS.jobs.create({name,cron,workflowType:'browser.navigate',payload:{url}}).then(refresh)}>Create</button></section><section className="panel"><h2>Jobs</h2>{state?.jobs.map(j=><article className="content-item" key={j.id}><div><b>{j.name}</b><p><code>{j.cron}</code> · {j.workflowType}</p></div><label><input type="checkbox" checked={j.enabled} onChange={e=>window.creatorOS.jobs.toggle(j.id,e.target.checked).then(refresh)}/> enabled</label></article>)}</section></div></div>}
