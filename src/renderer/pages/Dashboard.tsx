import React from 'react'; import type { AppState } from '../../shared/types';
export function Dashboard({state}:{state:AppState|null}) { return <div className="page"><h1>Dashboard</h1><p className="muted">Local-first creator operations workspace.</p><div className="cards"><div className="card"><strong>{state?.profiles.length??0}</strong><span>Profiles</span></div><div className="card"><strong>{state?.tabs.length??0}</strong><span>Browser tabs</span></div><div className="card"><strong>{state?.contents.length??0}</strong><span>Contents</span></div><div className="card"><strong>{state?.jobs.filter(j=>j.enabled).length??0}</strong><span>Active jobs</span></div></div><section className="panel"><h2>System guarantees</h2><p>Browser sessions use <code>persist:</code> partitions. Agent/browser automation is routed through the internal BrowserKernel; no external Chrome is launched by this MVP.</p></section></div>; }
