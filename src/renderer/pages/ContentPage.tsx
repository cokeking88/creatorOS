import React,{useMemo,useState} from 'react'; import type { AppState } from '../../shared/types';
export function ContentPage({state,refresh}:{state:AppState|null;refresh:()=>void}) {
  const [title,setTitle]=useState(''); const [body,setBody]=useState(''); const [platform,setPlatform]=useState('xiaohongshu'); const [accountId,setAccountId]=useState('');
  const platformRow=state?.platforms.find(p=>p.key===platform); const accounts=useMemo(()=>state?.accounts.filter(a=>a.platformId===platformRow?.id)||[],[state?.accounts,platformRow?.id]);
  return <div className="page"><h1>Content</h1><div className="content-grid"><section className="panel"><h2>New draft</h2><select className="field" value={platform} onChange={e=>{setPlatform(e.target.value);setAccountId('')}}>{state?.platforms.map(p=><option key={p.id} value={p.key}>{p.name}</option>)}</select><select className="field" value={accountId} onChange={e=>setAccountId(e.target.value)}><option value="">Unassigned account</option>{accounts.map(a=><option key={a.id} value={a.id}>{a.name}</option>)}</select><input className="field" placeholder="Title" value={title} onChange={e=>setTitle(e.target.value)}/><textarea className="field" rows={9} placeholder="Body" value={body} onChange={e=>setBody(e.target.value)}/><button className="primary" onClick={()=>window.creatorOS.content.create({title:title||'Untitled',body,platform,accountId:accountId||null,status:'draft'}).then(()=>{setTitle('');setBody('');refresh()})}>Save draft</button></section><section className="panel"><h2>Library</h2>{state?.contents.map(c=><article className="content-item" key={c.id}><div><b>{c.title}</b><p>{c.platform}{c.accountId?` · ${state.accounts.find(a=>a.id===c.accountId)?.name??c.accountId}`:''} · {c.body.slice(0,100)}</p></div><span className="pill">{c.status}</span></article>)}</section></div></div>;
}
